# =====================================================================
# 스마트 다이빙 장비 관리 및 멀티 가격 견적 시스템 - 환경 변수 예시
# 이 파일을 복사해 .env.local 로 저장한 뒤 실제 값으로 채워주세요.
#   cp .env.local.example .env.local
# 값은 Supabase 대시보드 > Project Settings > API Keys 에서 확인합니다.
# https://supabase.com/dashboard/project/_/settings/api-keys
# .env.local 은 절대 git 에 커밋하지 마세요.
# =====================================================================

# Supabase 프로젝트 URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# 퍼블리셔블 키 (브라우저에 노출되어도 되는 공개 키, sb_publishable_... 형식)
# * 레거시 이름: NEXT_PUBLIC_SUPABASE_ANON_KEY (anon key)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx

# 시크릿 키 (서버 전용, 절대 브라우저/클라이언트에 노출 금지, sb_secret_... 형식)
# * 레거시 이름: SUPABASE_SERVICE_ROLE_KEY (service_role key)
# * RLS 를 우회해야 하는 서버 전용 관리자 작업(배치 등)에만 사용하세요.
SUPABASE_SECRET_KEY=sb_secret_xxxxxxxxxxxxxxxxxxxxxxxx

# (선택) 공급자 도장 이미지 등을 저장할 Supabase Storage 버킷 이름
NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET=stamps

# Anthropic(Claude) API 키 - 장비 카탈로그 PDF 자동 업로드 기능에서 사용
# https://console.anthropic.com/settings/keys 에서 발급
# 서버 전용입니다. 절대 브라우저/클라이언트에 노출하지 마세요. (NEXT_PUBLIC_ 접두사 사용 금지)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
